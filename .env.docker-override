# Overrides any values in .env that are being used for local development

# POSTGRES_PORT=5432
# POSTGRES_HOST=host.docker.internal

GEOSERVER_INTERNAL_HOST=http://geoserver
GEOSERVER_INTERNAL_PORT=8080
GEOSERVER_DATA_DIR=/stored_data/geoserver_data

MESSAGE_BROKER_HOST=message_broker
